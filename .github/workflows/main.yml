name: US Tech Shorts Bot
on:
  schedule:
    - cron: '0 12-23,0 * * *'
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run bot
        run: python bot.py
      - name: Upload video for review
        uses: actions/upload-artifact@v4
        with:
          name: final-shorts-video-${{ github.run_number }}
          path: final_shorts.mp4
