name: Tech Show US Bot
on:
  schedule:
    # America Eastern Time: 6AM to 10PM ET = 10:00 to 02:00 UTC (EDT is UTC-4)
    # Har 2 ghante me: 6AM, 8AM, 10AM, 12PM, 2PM, 4PM, 6PM, 8PM, 10PM ET
    - cron: '0 10,12,14,16,18,20,22,0,2 * * *'
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install System Deps
        run: sudo apt-get update && sudo apt-get install -y imagemagick ffmpeg

      - name: Install Python Deps
        run: |
          pip install --upgrade pip
          pip install Pillow==9.5.0
          pip install -r requirements.txt

      - name: Run Bot
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          YT_CLIENT_ID: ${{ secrets.YT_CLIENT_ID }}
          YT_CLIENT_SECRET: ${{ secrets.YT_CLIENT_SECRET }}
          YT_REFRESH_TOKEN: ${{ secrets.YT_REFRESH_TOKEN }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: python src/main.py

      - name: Upload Artifact on Fail/Success
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: output
          path: output/
